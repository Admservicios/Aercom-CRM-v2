/**
 * ============================================================
 * AERCOM CRM v2
 * UI Manager
 * ============================================================
 */

function initializeUI() {

    debugLog("Inicializando interfaz...");

    initializeModals();

    initializeEvents();

    debugLog("✔ UI lista");

}

/**
 * Inicializa todos los eventos globales
 */
function initializeEvents() {

    // Se irá completando durante la migración

}
